import 'package:flutter/material.dart';

class SongPlayerHeaderWidget extends StatelessWidget {
  const SongPlayerHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      
    ],);
  }
}